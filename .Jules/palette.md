## 2024-10-26 - Semantic Headings vs Visual Separators
**Learning:** Developers often use `---` followed by `###` to simulate the look of an H2 (header with line). This breaks heading hierarchy (skipping H2).
**Action:** Use native `##` which provides both the correct semantic level and the visual border in GitHub Markdown.
